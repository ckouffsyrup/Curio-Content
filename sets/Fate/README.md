# Example Set

Replace this folder with the first official set.

Required files:

- `set.json`
- `curios.json`
- `icon.png`
- One image per Curio inside `images/`

Set `enabled` to `true` in the root `manifest.json` only after all referenced files exist.
